#!/bin/sh
# TODO: create man.conf
cat << EOF
NAME
    upowerw - UPOWERW is a simple upower command wrapper.
SYNOPSIS
    upowerw [OPTIONS]...
DESCRIPTION
    -p, --percentage
        display remaining charge
    -c, --charged
        is charge
    -h, --help
        man page(here)
EOF